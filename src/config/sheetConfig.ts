/**
 * Paste your published Google Sheet CSV URL here (see CMS_SETUP.md for the
 * 5-minute setup). Leave it blank ('') to run entirely on the local
 * fallback catalog in src/data/products.ts — the app works fine either way,
 * this just controls whether it can sync live without a redeploy.
 */
export const SHEET_CSV_URL = '';
